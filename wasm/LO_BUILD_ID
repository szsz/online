# Pinned LibreOffice WASM build ID — points to artefacts at
# https://coolwasmfiles.z6.web.core.windows.net/lo-builds/<ID>/
# Format: YYYY-MM-DD-<run_number> (matches the libreoffice-core-wasm CI run on dev branch).
#
# Bump this file via a PR to dev. The validate-lo-build job in
# .github/workflows/wasm-ci.yml verifies the artefact exists before
# auto-merge fires. See wasm/CO-EDITING-ARCHITECTURE.md
# "Build & release policy" for the full workflow.
#
# Use the literal token __LATEST__ to follow lo-builds/latest.txt
# (convenient for development; pin a literal ID for reproducibility).
#
# History:
#   2026-04-26-3   first successful LO CI build
#   2026-05-03-18  git_sha b8763b46 ("WASM: weak stub for
#                  wasm_quiesce_wake_main in standalone soffice.js")
#   2026-05-08-20  git_sha dd239876 ("WASM: reserve
#                  LOK_CALLBACK_DOCUMENT_READY=75 protocol number").
#                  Reserved the enum value Online's kit/Kit.cpp
#                  forwards (#ifdef-guarded; emit is in 2026-05-12-21).
#   2026-05-12-21  git_sha 69c442f9 ("WASM: emit
#                  LOK_CALLBACK_DOCUMENT_READY from
#                  doc_registerCallback"). The kit's docready: text
#                  frame now fires for every cold load + hot-switch,
#                  unblocking regression-event-driven-docready.
#   2026-05-18-24  git_sha 00ece5fd ("WASM: pack de/en-US/es/fr
#                  langpacks + repoint submodules to upstream (online
#                  task #193)"). Bundles de/en-US/es/fr translations
#                  into instdir so the styles sidebar CAN localise.
#                  Pairs with kit-side switchdocument lang-respect
#                  (online dev 0945dea9) for the UI-chrome half of
#                  #193. The sidebar deck content (paragraph style
#                  names like "Heading 1") still rendered English
#                  because StyleList.cxx used the internal name.
#   2026-05-19-26  git_sha ffb379fd ("WASM #193: sidebar Styles deck
#                  shows localised DisplayName"). StyleList.cxx caches
#                  UNO DisplayName at tree-build time and uses it at
#                  set_text for the hierarchical TreeView. Probe
#                  showed sidebar STILL English — turned out
#                  treeview_NN selectors belong to the FLAT-LIST view
#                  (m_xFmtLb), not the TreeView (m_xTreeBox). Default
#                  view is flat.
#   2026-05-20-27  git_sha 055b3225 ("WASM #193 follow-up: flat-list
#                  view uses DisplayName too"). Extended the fix to
#                  m_xFmtLb. Probe STILL showed English at runtime
#                  because…
#   2026-05-20-28  git_sha da81f271 ("WASM #193: CustomRenderHdl
#                  fallback uses localised name"). Wired UNO
#                  DisplayName through every set_text + canvas-draw
#                  path. Iconview tiles now German.
#   2026-05-21-30  git_sha 63a63148 ("WASM #196: enable spell squiggle
#                  painting in LOK tile mode"). Root cause for #196:
#                  sw/source/core/text/inftxt.cxx:687 gated
#                  red-spell-squiggle painting on `OnWin() &&
#                  IsOnlineSpell()`. In LOK tile rendering OnWin()
#                  is false (no native window — output goes to
#                  VirtualDevice tile buffers), so the squiggle was
#                  suppressed entirely regardless of the
#                  .uno:SpellOnline option set by kit/Kit.cpp. Fix
#                  changes the gate to `(OnWin() ||
#                  comphelper::LibreOfficeKit::isActive())` matching
#                  the established pattern from viewsh.cxx:213.
#   2026-05-21-31  current pin — git_sha f77b564b ("WASM #193
#                  follow-up: drop UNO DisplayName indirection —
#                  use GetName() directly"). Final fix for #193
#                  treeview-deck gap: UNO `DisplayName` is the
#                  language-invariant English identifier (used as a
#                  stable programmatic key across locales), NOT a
#                  translation. Prior PRs threaded that English
#                  identifier through set_text, overriding the
#                  actually-localised name that pStyle->GetName()
#                  returns (SfxResId-translated at pool init via
#                  STR_POOLCOLL_* keys). The treeview-deck now uses
#                  GetName() directly (matches what
#                  StylesPreviewWindow's lcl_AppendParaStyles does
#                  for the iconview). Selection lookup goes via
#                  get_selected_text() → Find(text), which also
#                  matches on GetName() — so id/text equality stays
#                  consistent. Removes the now-unused
#                  lcl_GetLocalisedStyleName helper. Build 31
#                  contains both #196 and #193 fixes.
2026-05-21-31
